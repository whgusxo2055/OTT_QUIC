services:
  ott_quic:
    build: .
    container_name: ott_quic_dev
    ports:
      - "8080:8080"
      - "8443:8443/udp"
    volumes:
      - ./data:/app/data
      - ./web:/app/web:ro
    command: ["./build/ott_server"]
